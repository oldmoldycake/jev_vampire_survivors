# Copy this file to .env (already git-ignored) and fill in your key.
# Get a key from TypeSafe (https://typesafe.ai/) once you have access to the Jev model.
# Without this set, the brain still runs but every decision falls back to a simple heuristic.
TYPESAFE_API_KEY=
